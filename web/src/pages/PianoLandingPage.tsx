// Page wrapper pour le projet Lovable Piano Play (Celsius).
// Adapte de TanStack Router vers React Router en gardant le composant intact.
import "../lovable-imports/lovable.css";
import Piano from "../lovable-imports/piano/pages/Piano";

export default function PianoLandingPage() {
  return <Piano />;
}
