name = "lunalive-tiktok-discovery"
main = "src/index.ts"
compatibility_date = "2025-12-17"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[browser]
binding = "BROWSER"

# Set via: wrangler secret put DISCOVERY_TOKEN
# Then call with header: x-tiktok-discovery-token: <same value>
